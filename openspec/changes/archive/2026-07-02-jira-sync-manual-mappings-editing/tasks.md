## 1. Implement Mapping State Update Callback

- [x] 1.1 Add `handleUpdateMapping` callback in `MigrationDashboard.tsx` to save manually edited mappings to `localStorage` and update issues state

## 2. Implement Inline Mapping Editing UI in IssueRow

- [x] 2.1 Add inline editing state, text input fields, and action buttons (Edit, Delete, Link, Save, Cancel) in the `IssueRow` component
- [x] 2.2 Wire up action button click events to update states and execute the mapping callback
